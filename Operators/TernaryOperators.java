package Operators;

public class TernaryOperators {
    public static void main(String[] args) {
        // int number = 5;
        // String type = ((number%2)==0) ? "Even":"Odd";
        // System.out.println(type);

        int marks = 32;
        String reporCard = marks >= 33 ? "PASS" : "FAIL";
        System.out.println(reporCard);
    }
}
